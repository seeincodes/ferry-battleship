// import { initializeGrid } from "./utils/gameHelpers";

// test("initializeGrid creates a grid of the correct size", () => {
//   const size = 10;
//   const grid = initializeGrid(size);
//   expect(grid.length).toBe(size);
//   grid.forEach((row) => {
//     expect(row.length).toBe(size);
//     row.forEach((cell) => {
//       expect(cell).toEqual({ isShip: false, isHit: false });
//     });
//   });
// });
